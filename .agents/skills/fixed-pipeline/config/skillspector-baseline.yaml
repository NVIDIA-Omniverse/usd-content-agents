version: 2
rules:
  - id: "PE3"
    path: "references/deploy-collection/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation uses one fixed generated Compose configuration path; it does not search for arbitrary credential files or print secret values."
  - id: "PE3"
    path: "references/deploy-embeddings-brev/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation reads one explicitly named operator configuration file and transfers only named API variables to an owner-only remote file without logging their values."
  - id: "PE3"
    path: "references/deploy-image-gen-brev/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation reads one explicitly named operator configuration file and transfers only named API variables to an owner-only remote file without logging their values."
  - id: "PE3"
    path: "references/deploy-material-agent-brev/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed synchronization exclusions prevent deployment configuration and credential files from being copied to the remote host."
  - id: "PE3"
    path: "references/deploy-material-agent-docker/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation uses one explicitly named local provider configuration file; it does not discover arbitrary credential files or print configured values."
  - id: "PE3"
    path: "references/deploy-physics-agent-brev/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed synchronization exclusions prevent deployment configuration and credential files from being copied to the remote host."
  - id: "PE3"
    path: "references/deploy-physics-agent-docker/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation uses one explicitly named local provider configuration file; it does not discover arbitrary credential files or print configured values."
  - id: "PE3"
    path: "references/deploy-qwen-vlm-brev/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation reads one explicitly named operator configuration file and transfers only the named registry variable to an owner-only remote file without logging its value."
  - id: "PE3"
    path: "references/deploy-qwen-vlm-brev/reference.md"
    message: "*known[_]hosts*"
    reason: "Reviewed SSH setup writes only the resolved service host key to the standard host-key registry; it does not access operator credentials or enumerate credential files."
  - id: "PE3"
    path: "references/deploy-qwen-vlm-brev/references/qwen36-h100.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation passes one explicitly named owner-only registry configuration file to the selected container."
  - id: "PE3"
    path: "references/deploy-texture-agent-brev/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation reads one explicitly named operator configuration file and transfers only named API variables to an owner-only remote file without logging their values."
  - id: "PE3"
    path: "references/deploy-texture-agent-docker/reference.md"
    message: "*.[e]nv*"
    reason: "Reviewed deployment documentation uses one explicitly named local provider configuration file; it does not discover arbitrary credential files or print configured values."
  - id: "RP1"
    path: "references/deploy-image-gen-brev/reference.md"
    reason: "Reviewed deployment command uses an explicitly tagged image; the scanner mistakes the option value before the image for the image reference."
  - id: "RP1"
    path: "references/deploy-material-agent-brev/reference.md"
    reason: "Reviewed deployment command runs an image built locally by the immediately preceding build step; the scanner mistakes the option value before the image for the image reference."
  - id: "RP1"
    path: "references/deploy-material-agent-docker/reference.md"
    reason: "Reviewed deployment command runs an image built locally by the documented build step; the scanner mistakes the option value before the image for the image reference."
  - id: "RP1"
    path: "references/deploy-ovrtx-docker/reference.md"
    reason: "Reviewed deployment commands use explicitly tagged images; the scanner mistakes option values before the image for image references."
  - id: "RP1"
    path: "references/deploy-physics-agent-brev/reference.md"
    reason: "Reviewed deployment command runs an image built locally by the immediately preceding build step; the scanner mistakes the option value before the image for the image reference."
  - id: "RP1"
    path: "references/deploy-physics-agent-docker/reference.md"
    reason: "Reviewed deployment command runs an image built locally by the documented build step; the scanner mistakes the option value before the image for the image reference."
  - id: "RP1"
    path: "references/deploy-qwen-vlm-brev/reference.md"
    reason: "Reviewed deployment commands use an explicitly tagged image; the scanner mistakes option values before the image for image references."
  - id: "RP1"
    path: "references/deploy-qwen-vlm-brev/references/qwen36-h100.md"
    reason: "Reviewed deployment command uses an explicitly tagged image; the scanner mistakes an option value before the image for the image reference."
  - id: "RP1"
    path: "references/deploy-texture-agent-brev/reference.md"
    reason: "Reviewed deployment commands run an image built locally by the documented build step; the scanner mistakes option values before the image for image references."
  - id: "RP1"
    path: "references/deploy-texture-agent-docker/reference.md"
    reason: "Reviewed deployment command runs an image built locally by the documented build step; the scanner mistakes the option value before the image for the image reference."
  - id: "LP3"
    path: "SKILL.md"
    reason: "Reviewed skill uses the repository-standard tools metadata to declare Shell, Filesystem, and Python capabilities; the scanner only recognizes its experimental allowed-tools field."
  - id: "AR2"
    path: "references/material-agent-cli/evals/fixtures/prefailed-resume/dataset/dataset.json"
    message: "D[O] NOT judge"
    reason: "Reviewed eval fixture directs material classification to use reference-image evidence instead of rendered placeholder material; it does not suppress safety warnings or refusals."
  - id: "AR2"
    path: "references/material-agent-cli/references/config-template.yaml"
    message: "D[O] NOT judge"
    reason: "Reviewed material-classification template preserves the product prompt that treats render appearance as non-authoritative and reference images as material evidence; it does not suppress safety warnings or refusals."
  - id: "TM1"
    path: "references/material-agent-client/evals/evals.json"
    message: "DE[L]ETE /sessions/{id}"
    reason: "Reviewed eval cleanup deletes only the case-owned service session through the documented resource-specific endpoint."
  - id: "TM1"
    path: "references/physics-agent-client/evals/evals.json"
    message: "DE[L]ETE /sessions/{id}."
    reason: "Reviewed eval cleanup deletes only the case-owned service session through the documented resource-specific endpoint."
fingerprints: []
