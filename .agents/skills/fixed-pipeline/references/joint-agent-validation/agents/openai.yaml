interface:
  display_name: "Fixed Pipeline: Joint Agent Validation"
  short_description: "Fixed pipeline: Run Gate 3A and Gate 3B on Joint output"
  default_prompt: "Use $fixed-pipeline to run Isaac Sim Asset Validator and SimReady Foundation validation on a Joint Agent USD or USDZ output, preserving raw findings and Research Preview limitations."
