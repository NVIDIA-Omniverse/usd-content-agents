diff --git a/CMakeLists.txt b/CMakeLists.txt
index 50ea31a..af97f67 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -125,6 +125,10 @@ option(TBB_DISABLE_HWLOC_AUTOMATIC_SEARCH "Disable HWLOC automatic search by pkg
 option(TBB_ENABLE_IPO "Enable Interprocedural Optimization (IPO) during the compilation" ON)
 option(TBB_CONTROL_FLOW_GUARD "Enable Control Flow Guard (CFG) during the compilation" OFF)
 option(TBB_FUZZ_TESTING "Enable fuzz testing" OFF)
+option(TBB_ENABLE_ITT_NOTIFY "Enable runtime loading of Intel ITT notification tools" ON)
+option(TBB_ENABLE_OPTIONAL_DYNAMIC_LOADING
+       "Enable runtime loading of optional allocator, affinity, RML, TCM, and OpenMP helpers"
+       ON)
 option(TBB_INSTALL "Enable installation" ON)
 option(TBB_FILE_TRIM "Enable __FILE__ trim" ON)
 if(LINUX)
diff --git a/src/tbb/CMakeLists.txt b/src/tbb/CMakeLists.txt
index 14ef766..7f77147 100644
--- a/src/tbb/CMakeLists.txt
+++ b/src/tbb/CMakeLists.txt
@@ -65,12 +65,13 @@ target_compile_definitions(
     $<$<NOT:$<BOOL:${BUILD_SHARED_LIBS}>>:__TBB_DYNAMIC_LOAD_ENABLED=0>
     $<$<NOT:$<BOOL:${BUILD_SHARED_LIBS}>>:__TBB_SOURCE_DIRECTLY_INCLUDED=1>
     $<$<NOT:$<BOOL:${TBB_VERIFY_DEPENDENCY_SIGNATURE}>>:__TBB_SKIP_DEPENDENCY_SIGNATURE_VERIFICATION=1>
+    $<$<NOT:$<BOOL:${TBB_ENABLE_OPTIONAL_DYNAMIC_LOADING}>>:__TBB_DYNAMIC_LOAD_ENABLED=0>
+    $<$<NOT:$<BOOL:${TBB_ENABLE_OPTIONAL_DYNAMIC_LOADING}>>:__TBB_WEAK_SYMBOLS_PRESENT=0>
 )
 
-if (NOT ("${CMAKE_SYSTEM_PROCESSOR}" MATCHES "(armv7-a|aarch64|mips|arm64|riscv)" OR
-         "${CMAKE_OSX_ARCHITECTURES}" MATCHES "arm64" OR
-         WINDOWS_STORE OR
-         TBB_WINDOWS_DRIVER))
+if (TBB_ENABLE_ITT_NOTIFY AND
+    NOT ("${CMAKE_SYSTEM_PROCESSOR}" MATCHES "(armv7-a|aarch64|mips|arm64|riscv)" OR
+         "${CMAKE_OSX_ARCHITECTURES}" MATCHES "arm64" OR WINDOWS_STORE OR TBB_WINDOWS_DRIVER))
     target_compile_definitions(tbb PRIVATE __TBB_USE_ITT_NOTIFY)
 endif()
 
diff --git a/src/tbb/allocator.cpp b/src/tbb/allocator.cpp
index 689c512..e8cbd0c 100644
--- a/src/tbb/allocator.cpp
+++ b/src/tbb/allocator.cpp
@@ -99,6 +99,7 @@ cache_aligned_allocate_handler_type cache_aligned_allocate_handler_unsafe = null
 //! Handler for padded memory deallocation
 static void (*cache_aligned_deallocate_handler)(void* p) = nullptr;
 
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
 //! Table describing how to link the handlers.
 static const dynamic_link_descriptor MallocLinkTable[] = {
     DLD(scalable_malloc, allocate_handler_unsafe),
@@ -126,6 +127,7 @@ static const dynamic_link_descriptor MallocLinkTable[] = {
 #else
 #error Unknown OS
 #endif
+#endif /* __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT */
 
 //! Initialize the allocation/free handler pointers.
 /** Caller is responsible for ensuring this routine is called exactly once.
@@ -133,7 +135,11 @@ static const dynamic_link_descriptor MallocLinkTable[] = {
     If that allocator is not found, it links to malloc and free. */
 void initialize_handler_pointers() {
     __TBB_ASSERT(allocate_handler == &initialize_allocate_handler, nullptr);
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
     bool success = dynamic_link(MALLOCLIB_NAME, MallocLinkTable, 4);
+#else
+    constexpr bool success = false;
+#endif
     if(!success) {
         // If unsuccessful, set the handlers to the default routines.
         // This must be done now, and not before FillDynamicLinks runs, because if other
diff --git a/src/tbb/governor.cpp b/src/tbb/governor.cpp
index 7492c0b..4a59346 100644
--- a/src/tbb/governor.cpp
+++ b/src/tbb/governor.cpp
@@ -392,6 +392,7 @@ static void (*restore_affinity_ptr)( binding_handler* handler_ptr, int slot_num
 int (*get_default_concurrency_ptr)( int numa_id, int core_type_id, int max_threads_per_core )
     = dummy_get_default_concurrency;
 
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
 #if _WIN32 || _WIN64 || __unix__ || __APPLE__
 
 // Table describing how to link the handlers.
@@ -430,6 +431,7 @@ static const unsigned LinkTableSize = sizeof(TbbBindLinkTable) / sizeof(dynamic_
 #define TBBBIND_2_0_NAME        LIBRARY_PREFIX "tbbbind_2_0"        DEBUG_SUFFIX LIBRARY_EXTENSION
 #define TBBBIND_2_5_NAME        LIBRARY_PREFIX "tbbbind_2_5"        DEBUG_SUFFIX LIBRARY_EXTENSION
 #endif /* _WIN32 || _WIN64 || __unix__ */
+#endif /* __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT */
 
 // Representation of system hardware topology information on the TBB side.
 // System topology may be initialized by third-party component (e.g. hwloc)
@@ -447,6 +449,7 @@ int* numa_nodes_indexes = nullptr;
 int  core_types_count = 0;
 int* core_types_indexes = nullptr;
 
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
 const char* load_tbbbind_shared_object() {
 #if _WIN32 || _WIN64 || __unix__ || __APPLE__
 #if _WIN32 && !_WIN64
@@ -463,6 +466,7 @@ const char* load_tbbbind_shared_object() {
 #endif /* _WIN32 || _WIN64 || __unix__ || __APPLE__ */
     return nullptr;
 }
+#endif /* __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT */
 
 int processor_groups_num() {
 #if _WIN32
@@ -479,6 +483,7 @@ int processor_groups_num() {
 void initialization_impl() {
     governor::one_time_init();
 
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
     if (const char* tbbbind_name = load_tbbbind_shared_object()) {
         initialize_system_topology_ptr(
             processor_groups_num(),
@@ -489,6 +494,7 @@ void initialization_impl() {
         PrintExtraVersionInfo("TBBBIND", tbbbind_name);
         return;
     }
+#endif /* __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT */
 
     static int dummy_index = automatic;
 
diff --git a/src/tbb/misc_ex.cpp b/src/tbb/misc_ex.cpp
index 03b33b4..b62b196 100644
--- a/src/tbb/misc_ex.cpp
+++ b/src/tbb/misc_ex.cpp
@@ -55,14 +55,14 @@ namespace r1 {
 
 #if __TBB_USE_OS_AFFINITY_SYSCALL
 
-#if __unix__
+#if __unix__ && (__TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT)
 // Handlers for interoperation with libiomp
 static int (*libiomp_try_restoring_original_mask)();
 // Table for mapping to libiomp entry points
 static const dynamic_link_descriptor iompLinkTable[] = {
     DLD_NOWEAK( kmp_set_thread_affinity_mask_initial, libiomp_try_restoring_original_mask )
 };
-#endif
+#endif /* __unix__ && optional dynamic loading */
 
 static void set_thread_affinity_mask( std::size_t maskSize, const basic_mask_t* threadMask ) {
 #if __FreeBSD__ || __NetBSD__ || __OpenBSD__
@@ -158,7 +158,7 @@ static void initialize_hardware_concurrency_info () {
     if ( !err ) {
         // We have found the mask size and captured the process affinity mask into processMask.
         num_masks = numMasks; // do here because it's needed for affinity_helper to work
-#if __unix__
+#if __unix__ && (__TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT)
         // For better coexistence with libiomp which might have changed the mask already,
         // check for its presence and ask it to restore the mask.
         dynamic_link_handle libhandle;
diff --git a/src/tbb/rml_tbb.cpp b/src/tbb/rml_tbb.cpp
index d1cd285..dff1c66 100644
--- a/src/tbb/rml_tbb.cpp
+++ b/src/tbb/rml_tbb.cpp
@@ -72,6 +72,7 @@ const ::rml::versioned_object::version_type CLIENT_VERSION = 2;
 ::rml::factory::status_type FACTORY::open() {
     // Failure of following assertion indicates that factory is already open, or not zero-inited.
     __TBB_ASSERT_EX( !library_handle, nullptr);
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
     status_type (*open_factory_routine)( factory&, version_type&, version_type );
     dynamic_link_descriptor server_link_table[4] = {
         DLD(__RML_open_factory,open_factory_routine),
@@ -88,6 +89,10 @@ const ::rml::versioned_object::version_type CLIENT_VERSION = 2;
         library_handle = nullptr;
         result = st_not_found;
     }
+#else
+    library_handle = nullptr;
+    status_type result = st_not_found;
+#endif
     return result;
 }
 
diff --git a/src/tbb/tcm_adaptor.cpp b/src/tbb/tcm_adaptor.cpp
index 7fa71c3..92c2e57 100644
--- a/src/tbb/tcm_adaptor.cpp
+++ b/src/tbb/tcm_adaptor.cpp
@@ -58,6 +58,7 @@ tcm_result_t(*tcm_register_thread)(tcm_permit_handle_t permit_handle){nullptr};
 tcm_result_t(*tcm_unregister_thread)(){nullptr};
 tcm_result_t (*tcm_get_version_info)(char* buffer, uint32_t buffer_size){nullptr};
 
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
 static const dynamic_link_descriptor tcm_link_table[] = {
     DLD(tcmConnect, tcm_connect),
     DLD(tcmDisconnect, tcm_disconnect),
@@ -90,6 +91,7 @@ static const dynamic_link_descriptor tcm_link_table[] = {
 #endif /* __unix__ */
 
 #define TCMLIB_NAME LIBRARY_PREFIX "tcm" DEBUG_SUFFIX LIBRARY_EXTENSION
+#endif /* __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT */
 
 static bool tcm_functions_loaded{ false };
 }
@@ -242,12 +244,16 @@ tcm_result_t renegotiation_callback(tcm_permit_handle_t, void* client_ptr, tcm_c
 }
 
 void tcm_adaptor::initialize() {
+#if __TBB_DYNAMIC_LOAD_ENABLED || __TBB_WEAK_SYMBOLS_PRESENT
     constexpr std::size_t tcm_link_table_size = sizeof(tcm_link_table) / sizeof(tcm_link_table[0]);
     // Allow system loader to search for TCM in the environment rather than load it from certain
     // location.
     constexpr int flags = DYNAMIC_LINK_DEFAULT & ~DYNAMIC_LINK_BUILD_ABSOLUTE_PATH;
     tcm_functions_loaded = dynamic_link(TCMLIB_NAME, tcm_link_table, tcm_link_table_size,
                                         /*handle*/nullptr, flags);
+#else
+    tcm_functions_loaded = false;
+#endif
 }
 
 bool tcm_adaptor::is_initialized() {
