# Modification notice

Each arm began from the recorded original source for its case and submitted the
USD and bindings included here. The USD files contain author-produced conversion,
scene organization and articulation/physics authoring for the experiment task.
They must not be represented as original upstream source files. Differences in
materials, grouping, joints, mass, collision representation and other authored
properties are part of each submission; this notice does not certify their
correctness or equivalence. Raw author scripts and model transcripts are excluded.

This publication step made **no changes** to geometry, materials, physics, bindings,
textures, dependency paths or any submitted bytes. All 11 payload hashes are
identical to their retained original archive members. Three drawer textures in
the plain arm also match the original Poly Haven files exactly.

The Content Agents drawer and conveyor layers retain generated provenance lines
in `rootLayer.documentation` naming canonical `/work/...` paths. Those strings
are metadata, not asset dependencies; they contain no user/host locator. The
manifest binds each disposition to the exact file, field and value digest. This
is not permission to expose arbitrary private paths or authorization fields.

The `README.md`, this notice, `SOURCE_NOTICES.md`, manifest and verifier were added
for publication. The upstream conveyor LICENSE/README were copied byte-for-byte.
No independent evaluation was read or rerun to prepare this artifact subset.
