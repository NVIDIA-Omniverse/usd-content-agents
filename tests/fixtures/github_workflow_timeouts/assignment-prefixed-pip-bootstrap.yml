name: assignment-prefixed raw pip bootstrap
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Unretried pip install with a leading assignment
        timeout-minutes: 15
        run: PIP_DISABLE_PIP_VERSION_CHECK=1 python -m pip install example-tool
