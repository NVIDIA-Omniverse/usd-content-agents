name: environment-prefixed raw pip bootstrap
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Unretried pip install with environment prefixes
        timeout-minutes: 15
        run: env PIP_DISABLE_PIP_VERSION_CHECK=1 python -m pip install example-tool
