name: malformed retry invocation
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Retry helper without required shell contract
        run: >-
          ci/github/retry-network-command.sh --
          python -m pip install example-tool
