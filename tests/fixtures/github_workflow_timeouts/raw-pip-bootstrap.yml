name: raw pip bootstrap
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Unretried pip install
        timeout-minutes: 15
        run: python -m pip install example-tool
