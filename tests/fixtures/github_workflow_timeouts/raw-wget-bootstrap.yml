name: raw wget bootstrap
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Unbounded wget
        timeout-minutes: 15
        run: wget https://example.invalid/tool.tar.gz
