name: retried bootstrap missing timeout
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Retried but step-unbounded install
        run: >-
          bash ci/github/retry-network-command.sh --
          python -m pip install example-tool
