name: sudo-prefixed raw pip bootstrap
on: push
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Unretried pip install with sudo options
        timeout-minutes: 15
        run: sudo -E python -m pip install example-tool
